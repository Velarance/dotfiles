# Device-specific configuration
# Copy this file to local.conf and customize per machine (local.conf is git-ignored).
#
# Monitor layout is managed by nwg-displays in ~/.config/hypr/monitors.conf.
# Put other per-machine overrides here, e.g.:
#   env = SOME_VAR,value
